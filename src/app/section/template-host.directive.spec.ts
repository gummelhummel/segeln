import { TemplateHostDirective } from './template-host.directive';

describe('TemplateHostDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateHostDirective();
    expect(directive).toBeTruthy();
  });
});
